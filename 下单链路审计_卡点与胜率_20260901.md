# HCM_V2 下单触发链路审计：闸门卡点与胜率分析

- 日期：2026-09-01
- 数据窗口：近 30 天（2026-08-02 ~ 09-01）为主；胜率用**全量历史**（2026-07-17 ~ 09-01）
- 性质：**只读审计** —— 未改动任何代码、配置、数据库、服务
- 行号核验：关键闸门（grade@1578、min_grade@1624、rule_chain 闸门顺序@175/187、order_send volume@1551、最小手数钳制@1276）已逐条 `sed` 抽检确认

---

## 0. 结论速览

| 维度 | 结论 |
|---|---|
| 链路完整性 | 5 层、**50+ 道闸门**，链路打通。当前**仅 HEXP 在产信号**，co_source 已整体下线 |
| 信号→下单 | 30 天 **6449 条信号 → 858 条成单（13.3%）**；**57.6% 死在信号塔**，11.6% 死在风控 |
| 最大卡点 | `hexp_grade_red` 19.3% → `hexp_grade_below_min` 11.2% → `below_threshold` 5.6% → `hexp_extreme_guard` 5.2% → `risk_cool_minutes` 4.8% |
| **胜率** | 已平仓 1157 笔：**胜 32.07% / 保本 41.14% / 亏 26.79%**；剔除保本后**决策胜率 54.5%** |
| **盈亏** | 总额 **-162.50**，单笔期望 **-0.14**；**盈亏比 0.75**（均盈 3.90 vs 均亏 5.19）→ 略微负期望 |
| **核心症结** | ① HEXP 信号 **81%~98% 以保本收场**（移动止损过紧，几乎不产生真实盈亏）；② **SELL 方向显著跑输**（-290 vs BUY +128）；③ 亏损集中在 `-5~-20` 中额区间（105 笔 -999），盈利被 `+5~+20` 封顶 |
| 架构性隐患 | **grade 在否决闸门之前评出，且闸门不回改 grade** → 面板显示 A 级 ≠ 会进下单链路 |

---

## 1. 链路全景

```
┌ 信号塔 (hcm-signal-tower, docker) ────────────────────────────────┐
│ bar 收盘: _symbol_loop(1049) → _wait_bar_close(1486) → _produce_signal  │
│ bar 内实时: _live_score_publisher(1279,3s) → hexp.produce(live,1342)    │
│             ├─ C层护栏(1369-1416) → _produce_signal(live_override,1404) │
│             └─ _live_override_loop(1196, ADX救援) → _produce_signal      │
│                                                                          │
│ _produce_signal 内部顺序:                                                │
│  指标(1768) → regime(1843) → active_model(1860) → HEXP produce(1879)     │
│  → 评 grade(1578) → min_grade 闸门(1624) → 安全护栏A11-A16(1637+)        │
│  → AI 闸门(2077) → threshold_passed(2177) → cooldown(2207)              │
│  → composite(2276) → zone(2553-2618) → SignalData(2808) → publish(2889) │
└──────────────────────┬───────────────────────────────────────────────┘
                       ▼  Redis Stream: signal:stream  (signal_publisher.py:351)
┌ 风控引擎 (hcm-risk-engine, docker) ───────────────────────────────┐
│ stream_consumer → _apply_dynamic_lot(105-194, 算手数) → rule_chain      │
│ 10 道闸门短路执行 → 通过发 risk:passed；拒绝写回 signals.block_reason   │
└──────────────────────┬───────────────────────────────────────────────┘
                       ▼  Redis Stream: signal:risk_passed
┌ MT5 桥 (tools/mt5_bridge.py, 宿主 Windows 进程，非 docker) ────────┐
│ 批次latest-wins(3743) → 账号过滤(3770) → status/is_active(3821/3846)    │
│ → 乱序守卫(3868) → 新鲜度(4013) → 去重(4041) → 速率50/分(4065)          │
│ → zone延迟(4122) → ATR过滤(4136) → after_close_cooldown(1704)          │
│ → reverse_guard(1710) → position_cap(1742) → place_mt5_order(1199)     │
│ → order_send(1566, volume=lot@1551)                                     │
└────────────────────────────────────────────────────────────────────┘
        ↕ 跟单号独立消费同一 risk_passed 流（FOLLOW_LOT_MULT 缩放，BR:3232）
```

**引擎现状（代码实证）**：`co_source.py:1-17` 文件头标注「已下线」，全文仅剩共享常量；`scheduler.py:137-139` 已删除 co_source 导入；`_detect_active_model`(4105-4127) **任何异常/未知值一律回退 `"hexp"`**。→ 不存在落入 co_source 的路径。

---

## 2. 闸门清册

### 2.1 信号塔 · HEXP 引擎（`hexp_engine.py`）

| # | file:line | 判定 | 配置键（代码默认） | 效果 |
|---|---|---|---|---|
| A1 | 1032 | `if not cfg["hexp.enabled"]` | `hexp.enabled`=True | 直接 return |
| A2 | 1077 | `primary not in period_data` | `hexp.primary_period`="M5" | return |
| A3 | 1289 | `abs(dir_sum)<0.01 and max(\|v\|)<dmin` | `hexp.direction_min_score`=0.20 | NO_TRADE |
| A4 | 1303-1321 | 方向迟滞（防翻转） | `direction_hysteresis`=0.06 / `_strong`=0.20 | 维持旧方向 |
| A5 | 1458-1462 | MTF 共振罚分（**否决已删除，仅调分**） | `resonance.penalty`=0.15 | 降 hp |
| A7 | 1520-1525 | hp 斜率限幅 | `score.hp_delta_max`=15.0 | 降分 |
| A8 | 1542-1574 | zone 位置加/减分 | `zone.penalty_enabled`=False | 调分 |
| **A9** | **1578** | **`grade = _resolve_grade_hyst(...)`** | `a_threshold`=75 / `b_threshold`=52 / `pass_threshold`=50 / `s_hp_min`=60 | **评 grade** |
| **A10** | **1624-1629** | `_grade_ok = _GRADE_RANK[grade] >= _min_rank`；`passed = _grade_ok and direction!="NO_TRADE"` | `hexp.min_grade`="C" | **passed=False + NO_TRADE** |
| A11 | 1728-1752 | zone 阻挡 | `zone.block_enabled`=**False** | NO_TRADE（默认关） |
| A12 | 1681/1753-1790 | 极值周期位（`_pos_cycle>0.85` / `_pos_z>3.5`） | `extreme.high_pct`=0.85、`cycle.z_extreme`=3.5 | NO_TRADE；**保本持仓转 `extreme_pending` 交风控** |
| A13 | 1843-1866 | 极值反转撤退 | `extreme.reversal_enabled`=True、`mm_retreat_min`=0.20 | NO_TRADE / extreme_pending |
| A14 | 1884-1905 | 动量衰竭（`pos>0.65 & er<0.20 & mm<0.15`） | `momentum_drain_enabled`=True | NO_TRADE |
| **A15** | **1918-1983** | **动量翻转 `f_mm_s < -_eff_th`** | **`momentum_flip_mm`=0.04**（生产实测 Redis/PG=**0.01**）、`_trend_adx`=25 | **NO_TRADE** |
| A16 | 2068-2102 | 区间 Hurst（`_hurst<0.50`） | `range_hurst_enabled`=True | NO_TRADE |
| A17 | 2343-2385 | 趋势启动反向放行（**绕过 grade**） | `trend_start_order_enabled`=**False** | 默认关 |
| A18 | 2458-2467 | `_grade_ok=False` 终局兜底 | 同 A10 | 锁死 NO_TRADE |

> **架构性隐患**：A9 先评 grade，A11–A16 五道安全护栏**全部跑在 grade 之后**，且只改 `direction`/`passed`，**不回改 `grade`**（2458 行仅在 `_grade_ok=False` 时才动）。因此 **面板显示 A/S 级的信号，完全可能已被 momentum_flip / extreme_guard / cycle_pos 封成 NO_TRADE，而 `sr.grade` 仍显示 A/S**。这是"看到 A 级却不下单"的根因，也是排查时的头号误导源。

### 2.2 调度发射层（`scheduler.py`）

| # | file:line | 机制 | 配置键 | 效果 |
|---|---|---|---|---|
| B1 | 1683-1718 | **每棒幂等**：Redis `signal_tower:last_bar:{sym}:{tf}` TTL 86400 | 硬编码 | 同 bar 不重产（`live_override` 绕过 1720-1724） |
| B2 | 1754 | `len(klines)<30` | — | 跳过 |
| B3 | 1947-2019 | 微状态 `TREND_EXHAUST` | `entry_gate_enabled`=**False** | 默认关 |
| B4 | 2098-2104 | AI 否决 `VETO` | `ai.lm.veto_floor`、`dir_veto_prob`=0.65 | 整轮跳过 |
| B6 | 2159-2175 | 耦合通过 `coupling_pass is False` | `hexp.coupling_pass_threshold`=50.0 | 跳过 |
| B7 | 2177-2201 | `not threshold_passed` | — | 跳过 |
| B8 | 2216-2229 | **按 symbol 冷却** `elapsed < cooldown` | `fade_cooldown_seconds`=300、`trend_reverse_cooldown_seconds`=90 | 跳过 |
| B10 | 2341-2358 | `final_direction=="NO_TRADE"` | — | filtered |

**发射节流（实测关键）**：
1. **上升沿武装位** `SymbolState.live_edge_armed`(286，默认 True)：分值回落/方向翻转/方向消失 → 重新武装；持续在阈上 → **不发**。
2. **方向稳定计数**：`stable_count < trigger_stable_required` —— **局部变量 =2（scheduler.py:1300），非配置驱动**。
3. **限流**：`trigger_rate_limit_sec` —— **局部变量 =30 秒（1299），非配置驱动**；与 `_live_override_loop` 共享 `last_live_override_time`。
4. **单棒单发**：1391-1392 `last_live_trigger_bar == _cur_bar` → pass。

> 注：`trigger_stable_required=2` 与 `trigger_rate_limit_sec=30` 是**硬编码局部变量**，面板/配置中心调不到，属可观测性缺口。

### 2.3 风控引擎（`rule_chain.py`，短路执行，命中即 return）

判定顺序：**confidence → max_lot_single → max_total_lot → max_open_positions → max_daily_loss → min_margin → max_spread → cool_minutes → reverse_order → spread_check**（调用点依次 175 / 187 / 199 / 211 / 223 / 235 / 247 / 260 / 278 / 291）

| reason 字符串 | file:line | 条件 | 配置键（默认） | 效果 |
|---|---|---|---|---|
| `config_unloaded` | 159-172 | `not _config_loaded` | — | 拒单（fail-closed） |
| `risk_min_confidence` | 314 | `confidence >= min` | `risk_min_confidence`(0.0) | 拒单 |
| `risk_max_lot_single` | 334 | `lot <= max` | `risk.max_lot_per_trade`(0.0) | 拒单 |
| `risk_max_total_lot` | 365 | `existing+lot <= max` | `risk.max_total_exposure`(0.0)；DB故障哨兵 99999 | 拒单 |
| `risk_max_open_positions` | 425 | 同向 open 数 < 阈值 | `risk.max_concurrent_signals`(10)；分值档 0.50/0.70→1/2/3 | 拒单 |
| `risk_max_daily_loss` | 456 | `daily_loss >= -max` | `risk.max_daily_loss`(0.0) | 拒单（熔断） |
| `risk_min_margin` | 484 | `free_margin >= min` | `risk.margin_call_level`(0.0)；DB故障 **fail-open** | 拒单 |
| `risk_max_spread_pips` | 516 | `spread <= max` | `risk_spread_max_multiplier`(999) | **软规则 → DEGRADE 仍放行** |
| **`risk_cool_minutes`** | **629/594/719** | 同向保本：extreme_pending 未保本拒(629)、DB不可达拒(589-595) | `risk.cool_be_tolerance`(0)；源=Redis TTL15s(636)+PG(662) | **拒单（实测第 5 大卡点）** |
| `risk_reverse_order` | 792/827/838 | 有同向持仓未保本拒 | 同上 | 拒单／减仓 ×0.3(848) 或 ×0.5(862) |

> **⚠️ 隐患**：`stream_consumer.py:251` 启动自检 `CRITICAL_SAFETY_KEYS`（shared/redis_client.py:528-539）校验的是**下划线键**（`risk_max_open_positions`），而 rule_chain 实际读的是**点号键**（`risk.max_*`）→ **启动自检不覆盖真实闸门键**，防护形同虚设。

### 2.4 MT5 桥（`tools/mt5_bridge.py`）

| file:line | 检查 | 配置键 / 阈值 |
|---|---|---|
| 3690-3700 | MT5 不可用 | 跳过消费 |
| 3743-3747 | 批次 latest-wins | 非最新 DROP |
| 3770-3794 | 账号过滤（跟单号 `sig_account not in FOLLOW_MASTERS` 丢弃 @3777） | `FOLLOW_MASTERS` |
| 3821-3831 | `account.{id}.status != running` | — |
| 3834-3842 | `FOLLOW_CIRCUIT_BROKEN` | 跟单丢弃 |
| 3846-3857 | `account.{id}.is_active != true` | Redis 优先，PG 每 5s 重载 |
| **4013** | **信号新鲜度 `_is_signal_fresh`** | `bridge.max_signal_age_seconds`（缺失回退 180、下限 60）；时钟偏移容差 `max_clock_skew_sec`=180；落卡点 `bridge_signal_expired`@4018 |
| 4041 / 4050-4058 | 去重（Redis `bridge:processed:{acct}:{sid}` + PG positions∪orders） | — |
| 4065 | 速率 50 单/分 | — |
| 4122-4131 | zone 延迟 | `signal_tower.zone_trigger_enabled`，band 5.0pts |
| 4136-4141 | ATR 尖刺过滤 | `zone_atr_filter_enabled` / `_mult`=3.0ATR |
| 1704-1709 | after_close_cooldown | `close.after_close_cooldown_sec` |
| 1710-1736 | reverse_guard | `close.reverse_guard_enabled`=True |
| 1742-1766 | position_cap | `risk.max_concurrent_signals`，缺失回退 3 |
| 1230-1234 | `mt5_trade_ready`（`trade_allowed`） | 硬失败 |
| **1312-1322** | **ENTRY_MISSED 滑点** | `max_slip = ATR × bridge.max_entry_slippage_atr_mult`(0.3)，ATR 缺回退 `max_entry_slippage_pts`(50) |

### 2.5 手数链路（重点：0.5 倍轻仓会被吞）

```
信号塔 lot + suggested_lot_ratio
  → stream_consumer._apply_dynamic_lot(105-194)：base(risk.lot_base=0.01) × 分值倍率(2.0/1.0/0.5) × co_ai_mult
  → rule_chain 只改倍率：×0.5(极值追单@613) / ×0.5(@862) / ×0.3(@848)
  → BR:1237 lot = msg.lot（≤0→0.01）
  → BR:1241-1247 跟单 FOLLOW_LOT_MULT 缩放
  → BR:1252-1262 应用 suggested_lot_ratio
  → BR:1271-1285 最小手数钳制：lot < volume_min → lot = volume_min（1276-1278）
  → BR:1551 "volume": lot → BR:1566 order_send
```

> **实测隐患**：XAUUSD `volume_min` 通常 0.01，`base 0.01 × 2.0 × 0.5 = 0.01` 恰在边界 → **极值追单的 0.5 倍轻仓在 BR:1276-1278 被抬回最小手数，减半被吞掉**。即"轻仓"设计在最小手数边界上失效。

### 2.6 跟单链路

- **已去跟单化**：`copy_trading/stream_consumer.py:42` `COPY_TRADING_ORDER_EXECUTION_ENABLED=False`，仅消费+ACK 不下单；跟单由**跟单桥独立下单**（BR:3776-3790），手数按 `FOLLOW_LOT_MULT`(BR:3232-3242，来自 `hcm_copy.relationships.lot_multiplier`) 缩放。
- **每日盈亏熔断** BR:2082-2274：per-account 读 `max_daily_profit/max_daily_loss/circuit_break_enabled`(2118-2127)；分母 = 07:00 记录的 `bridge:follow:baseline:{acct}`；**双条件**（百分比达标 **且** 绝对额 ≥ `follow_daily_profit_abs_min`）(2200-2238)；触发 → 全平 + 写 `bridge:follow:circuit:{acct}`（TTL 至 `follow_resume_time`=06:30）。
- **SL/TP 毫秒镜像**：BR:2774-2830（`hcm:master:sltp`）+ 10s 兜底 BR:4222-4223。

---

## 3. 生产漏斗实测（近 30 天）

| 阶段 | 数量 | 占信号总数 | 说明 |
|---|---:|---:|---|
| 信号产出（`hcm_signal.signals`） | **6449** | 100% | 含 filtered / live 快照 |
| ├ 信号塔拦截（`signal_status=0`） | 3712 | **57.56%** | 最大漏损层 |
| ├ 已发布待处理（`status=1`） | 402 | 6.23% | |
| ├ 已发风控（`status=2`，其中被风控拒 318） | 1478 | 22.92% | 风控拒 318（11.62% 的已发布） |
| └ **成功成单（`status=3`）** | **858** | **13.30%** | |
| 实际订单数（`hcm_trading.orders`） | 1395 | — | 858 信号 × ~1.63（主号 + 跟单号） |
| 已平仓订单 | 1157 | — | 全量历史口径 |

**关键比例**：信号塔吃掉 **57.6%**，风控吃掉已发布量的 **11.6%**，最终 **13.3%** 成单。

> ⚠️ **数据一致性问题**：`order_status=1`（未平）订单 **653 条**，但 `hcm_trading.positions` 实际 open 仅 **8 个** → 约 645 条订单记录滞留"open"态。若风控的 `risk_max_open_positions` 读的是 orders 表而非 positions 表，会造成**虚假占额并拒单**（需另行验证读哪张表）。

---

## 4. 卡点排行（近 30 天，6449 条信号）

| 排名 | 卡点 | 数量 | 占比 | 层级 | 可优化性 |
|---:|---|---:|---:|---|---|
| — | **PASSED（通过）** | 2804 | 43.47% | — | — |
| 1 | `hexp_grade_red` | 1243 | **19.27%** | 信号塔 | 调 RED 阈值/降 hp_floor |
| 2 | `hexp_grade_below_min` | 719 | **11.15%** | 信号塔 | `hexp.min_grade`（现 C） |
| 3 | `below_threshold` | 359 | 5.57% | 信号塔 | `hexp.scorecard.pass_threshold`(0.45) |
| 4 | `hexp_extreme_guard` | 334 | 5.18% | 信号塔 | `extreme.*` 系列 |
| 5 | **`risk_cool_minutes`** | 310 | **4.81%** | **风控** | `risk.cool_be_tolerance`（现 0） |
| 6 | `hexp_momentum_flip` | 159 | 2.47% | 信号塔 | `momentum_flip_mm`（生产 0.01） |
| 7 | `v2_h1_counter_trend` | 112 | 1.74% | 信号塔 | H1 反向过滤 |
| 8 | `hexp_cycle_pos_guard` | 82 | 1.27% | 信号塔 | `cycle.z_extreme`=3.5 |
| 9 | `hexp_coupling_below_min` | 55 | 0.85% | 信号塔 | `coupling_pass_threshold`=50 |
| 10 | `no_trade_direction` | 41 | 0.64% | 信号塔 | |
| 11 | `cooldown_active` | 37 | 0.57% | 调度 | `fade_cooldown_seconds`=300 |
| 12 | `hexp_momentum_drain` | 32 | 0.50% | 信号塔 | |
| — | `risk_reverse_order` | 7 | 0.11% | 风控 | |
| — | `bridge_after_close_cooldown` | 4+8 | 0.18% | 桥 | |

**判读**：拦截几乎全部发生在**信号塔（约 48%）**，风控仅占 **~5%**。想提升下单量，应调信号塔阈值（grade / pass_threshold / extreme 系列）；风控侧唯一实质卡点是 `risk_cool_minutes`（同向保本闸门，`cool_be_tolerance`=0 即零容差，偏严）。

---

## 5. 胜率分析（全量 1157 笔已平仓）

### 5.1 总览

| 指标 | 值 |
|---|---|
| 已平仓笔数 | 1157 |
| 盈利 | 371（**32.07%**） |
| **保本（profit=0）** | **476（41.14%）** |
| 亏损 | 310（26.79%） |
| **决策胜率**（剔除保本） | **54.48%**（371/681） |
| 总盈亏 | **-162.50** |
| 单笔期望 | **-0.14** |
| 均盈 / 均亏 | **3.90 / 5.19** |
| **盈亏比** | **0.75** |
| 最大盈 / 最大亏 | +33.88 / -161.14 |

**判读**：决策胜率 54.5% 尚可，但**盈亏比仅 0.75**（赢 3.90 输 5.19）→ 净期望为负。系统"赢得稍多、但输得更狠"。

### 5.2 盈亏分布（症结所在）

| 区间 | 笔数 | 占比 | 盈亏 |
|---|---:|---:|---:|
| **保本 0** | 476 | 41.1% | 0.00 |
| 盈 0~5 | 273 | 23.6% | **+484.43** |
| 盈 5~20 | 91 | 7.9% | **+782.17** |
| 盈 >20 | 7 | 0.6% | +179.31 |
| 亏 -5~0 | 196 | 16.9% | -202.79 |
| **亏 -20~-5** | **105** | **9.1%** | **-999.36** |
| 亏 < -20 | 9 | 0.8% | -406.26 |

**核心矛盾**：**105 笔中额亏损（-5~-20）吃掉了 -999.36**，是全部盈利（+1445.91）的 69%。盈利被 `+5~+20` 区间封顶，而亏损有长尾 → **典型的"截断盈利、放任亏损"**。

### 5.3 按方向（**SELL 是主要拖累**）

| 方向 | 笔数 | 胜 | 胜率 | 盈亏 | 单笔均值 |
|---|---:|---:|---:|---:|---:|
| **BUY** | 712 | 261 | **36.66%** | **+127.70** | +0.18 |
| **SELL** | 445 | 110 | **24.72%** | **-290.20** | **-0.65** |

→ SELL 方向不只是胜率低 12 个百分点，单笔均值更是 -0.65（BUY 为 +0.18）。**若剔除 SELL，系统为 +127.70**。

### 5.4 按信号模式（**保本率是最大的结构性问题**）

| signal_mode | 笔数 | 胜率 | **保本率** | 盈亏 | 单笔均值 |
|---|---:|---:|---:|---:|---:|
| (无信号行/手工) | 446 | 46.64% | **0.22%** | **-194.12** | -0.44 |
| live_override | 349 | 15.47% | **81.09%** | **+161.70** | +0.46 |
| NEUTRAL_WEIGHTS | 105 | 51.43% | 18.10% | -29.36 | -0.28 |
| **HEXP:Regime.NEUTRAL** | 105 | **2.86%** | **96.19%** | -3.86 | -0.04 |
| **HEXP:Regime.TREND** | 60 | **1.67%** | **98.33%** | +15.78 | +0.26 |
| co_source | 37 | 72.97% | 0.00% | +18.20 | +0.49 |
| TREND_WEIGHTS | 27 | 55.56% | 0.00% | -101.18 | -3.75 |
| RANGE_WEIGHTS | 14 | 42.86% | 57.14% | +40.93 | +2.92 |
| gaoren_fusion | 6 | 0.00% | 0.00% | **-81.63** | **-13.61** |

**⚠️ 核心发现**：HEXP 主力路径（`Regime.TREND` / `Regime.NEUTRAL` / `live_override`）的**保本率高达 81%~98%**。即 HEXP 信号开出的仓，**绝大多数在移动止损推到保本后被扫出，既没赚也没亏** —— 引擎在"空转"。真实盈亏反而来自**非 HEXP 路径**（无信号行 -194、TREND_WEIGHTS -101、gaoren_fusion -82 贡献了几乎全部亏损）。

推论：`breakeven_floor`（`entry ± ATR × breakeven_buffer_atr_mult`，默认 0.15）与 trailing 过紧，导致**浮盈尚未展开就被 BE 锁定**。这是"胜率上不去"和"赚不到钱"的共同根因。

### 5.5 按 grade（**A/S 级单笔质量显著更好**）

| grade | 笔数 | 胜率 | 盈亏 | 单笔均值 |
|---|---:|---:|---:|---:|
| (无等级) | 1065 | 32.68% | **-243.75** | **-0.23** |
| A | 48 | 18.75% | +43.54 | **+0.91** |
| S | 32 | 21.88% | +31.07 | **+0.97** |
| B | 11 | 54.55% | +5.80 | +0.53 |
| C | 1 | 100% | +0.84 | +0.84 |

**判读**：A/S 级笔数极少（80 笔，占 6.9%），胜率看似更低（因为大量保本退出不计胜），但**单笔均值 +0.91/+0.97，远优于无等级的 -0.23**，且**合计为正（+74.61）**。→ **等级是有效的区分度信号，问题在于 A/S 级样本太少且被保本机制削平了赔率**。

### 5.6 按交易时段（UTC）

| 时段 | 盈亏 |
|---|---:|
| 亚洲（0-6h） | -92.50 |
| 欧洲（7-12h） | **+61.52** |
| **美国（13-19h）** | **-266.01** ← 最差 |
| 尾盘（20-23h） | **+134.49** ← 最好 |

单小时最差：`05h`(-121.20)、`17h`(-76.93)、`02h`(-51.24)、`18h`(-53.70)。→ **美盘（13-19 UTC）是系统性亏损时段**。

### 5.7 按账户

| account_id | 笔数 | 胜率 | 盈亏 | 均手数 |
|---:|---:|---:|---:|---:|
| 6 | 429 | 34.97% | **-264.26** | 0.013 |
| 23 | 321 | 19.63% | -19.22 | 0.015 |
| 9 | 248 | **60.08%** | **+188.56** | 0.012 |
| **24** | 150 | **2.00%** | -72.99 | 0.015 |
| 35 | 9 | 66.67% | +5.41 | 0.010 |

→ **账户 24（跟单号）胜率仅 2.00%**，与账户 9（60.08%）形成极端反差，需优先排查（该账户此前有每日盈亏熔断记录）。

### 5.8 按持仓时长

| 时长 | 笔数 | 胜率 | 盈亏 |
|---|---:|---:|---:|
| <5min | 614（53.1%） | 33.39% | **+128.96** |
| 5-15min | 270 | 32.22% | +59.47 |
| 15-60min | 230 | 29.13% | -121.53 |
| 1-4h | 36 | 27.78% | +2.93 |
| **4-24h** | **6** | 33.33% | **-215.77** ← 6 笔亏 216 |
| >24h | 1 | 0% | -16.56 |

→ **53% 的仓位 5 分钟内离场**（保本机制的直接体现）；而 4-24h 的 6 笔长持亏损 -215.77，说明**过夜/长持缺乏保护**。

### 5.9 按周趋势

| 周 | 笔数 | 胜率 | 盈亏 |
|---|---:|---:|---:|
| 07-13 | 27 | 37.04% | -37.90 |
| 07-20 | 287 | 35.19% | +19.41 |
| 07-27 | 101 | 82.18% | +75.22 |
| 08-03 | 210 | 66.67% | **-218.25** |
| **08-10** | **474** | **0.63%** | -55.86 |
| 08-17 | 38 | 63.16% | +106.64 |
| 08-24 | 20 | 50.00% | -51.76 |

→ **08-10 那周 474 笔仅 3 笔盈利**（其余几乎全保本）—— 保本空转最严重的一周。**08-03 周胜率 66.67% 却亏 -218.25** —— 高胜率被少数大亏单吞噬，再次印证盈亏比问题。

---

## 6. 关键风险与发现（按严重度）

| # | 严重度 | 发现 | 证据 |
|---:|---|---|---|
| 1 | **P0** | **grade 与闸门不同步**：grade 先评，否决闸门后跑且不回改 grade → 面板 A/S 级信号可能实为 NO_TRADE | hexp_engine.py:1578 vs 1624/1637+；A18@2458 仅 `_grade_ok=False` 时才动 |
| 2 | **P0** | **HEXP 信号 81%~98% 以保本收场**：移动止损推 BE 过紧，浮盈未展开即被扫 → 引擎空转，不产生真实 edge | 5.4 节保本率：Regime.TREND 98.33%、NEUTRAL 96.19%、live_override 81.09% |
| 3 | **P1** | **盈亏比 0.75，中额亏损长尾**：105 笔 -5~-20 吃掉 -999，盈利被 +5~+20 封顶 | 5.2 节 |
| 4 | **P1** | **SELL 方向系统性跑输**：胜率 24.7% vs BUY 36.7%，盈亏 -290 vs +128 | 5.3 节 |
| 5 | **P1** | **美盘（13-19 UTC）系统性亏损 -266** | 5.6 节 |
| 6 | **P1** | **订单表 653 条滞留 open 但实际仅 8 个持仓** → 若风控占额读 orders 表会造成虚假拒单 | 3 节；需验证 rule_chain:425 读哪张表 |
| 7 | **P2** | **风控启动自检键名不匹配**：自检下划线键 `risk_max_*`，代码读点号键 `risk.max_*` | stream_consumer.py:251 / shared/redis_client.py:528-539 vs rule_chain.py |
| 8 | **P2** | **极值追单 0.5 倍轻仓在最小手数边界被吞** | mt5_bridge.py:1276-1278（base 0.01×2.0×0.5=0.01 = volume_min） |
| 9 | **P2** | **发射节流参数硬编码**：`trigger_stable_required`=2、`trigger_rate_limit_sec`=30 为局部变量，配置中心不可调 | scheduler.py:1299-1300 |
| 10 | **P2** | **账户 24 胜率仅 2.00%** | 5.7 节 |
| 11 | **P3** | **平仓原因全部为 `sync_reconcile`**：无法区分 SL/TP/手动/保本离场，归因能力缺失 | 5.E 节 |
| 12 | **P3** | **`hcm_trade.closed_positions` 空表**：平仓流水未落该表，胜率只能靠 orders.profit | 查询 1/6 |

---

## 7. 优化建议（**均需单独授权，本次未执行**）

| 优先级 | 建议 | 预期收益 | 风险 | 授权 |
|---|---|---|---|---|
| **A** | **放宽保本/移动止损**：上调 `breakeven_buffer_atr_mult`（0.15→0.3+）与 `trail_wide_atr_mult`，让浮盈有展开空间 | 直击保本率 81-98% 的空转，最可能扭亏 | 中（回撤增大，需小步试） | 待授权 |
| **B** | **SELL 方向专项**：对 SELL 加额外过滤（如仅在 `Regime.TREND` + H1 同向时放行），或降 SELL 手数倍率 | 剔除 -290 的拖累 | 中 | 待授权 |
| **C** | **美盘限流/降仓**：13-19 UTC 降低手数倍率或直接过滤 | 规避 -266 的系统亏损 | 低 | 待授权 |
| **D** | **修 grade 与闸门不同步**：否决闸门命中时同步降级 `sr.grade`（或面板改读 `passed`/`direction`） | 消除"看到 A 级不下单"的误判 | 低（仅展示层或补写字段） | 待授权 |
| **E** | **查风控占额表**：确认 `rule_chain:425` 读 orders 还是 positions，若读 orders 需清理 645 条滞留记录 | 可能直接提升通过率 | 中（涉及数据清理） | 待授权 |
| **F** | **修启动自检键名**：把 `CRITICAL_SAFETY_KEYS` 改成点号键 | 让自检真正生效 | 低 | 待授权 |
| **G** | **放开卡点阈值**：`hexp.min_grade`、`pass_threshold`(0.45)、`risk.cool_be_tolerance`(0→正值) | 提升下单量（但可能降质） | 高（需 A/B） | 待授权 |
| **H** | **补平仓归因**：落 `hcm_trade.closed_positions` + 区分 close_reason（SL/TP/BE/手动） | 让后续归因可做 | 低 | 待授权 |

> **建议顺序**：先做 **D + H**（低成本、纯可观测性）→ 再做 **A**（最高收益）→ 观察 2 周 → 再评估 **B/C**。G 类阈值调整风险高，建议在 A 之后、有 A/B 数据支撑再做。

---

## 附：数据来源与口径

- 信号/订单/持仓：`hcm_signal.signals`、`hcm_trading.orders`、`hcm_trading.positions`、`hcm_trade.closed_positions`（空表）
- 胜率口径：`hcm_trading.orders` 中 `close_time IS NOT NULL` 的 1157 笔，`profit>0` 计胜、`=0` 计保本、`<0` 计亏
- 卡点口径：`hcm_signal.signals` 近 30 天，按 `block_reason`（风控）/ `fallback_reason`（信号塔，`split_part(...,'(',1)` 归一化）分类
- 时段口径：UTC 小时；亚洲 0-6、欧洲 7-12、美国 13-19、尾盘 20-23
- 全程只读，未执行任何 INSERT/UPDATE/DELETE/配置变更/服务重启
