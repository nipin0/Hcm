# LightGBM 反转头 · 每日绩效报表设计方案

> 日期：2026-09-04（GMT+8）
> 性质：只读侦察 + 设计文档，**未改动任何生产代码/配置/表结构**
> 目标：为「趋势末端浮亏持仓 反转 vs 回踩」LightGBM 反转头建立**每日绩效报表**，用硬数据回答一个决策问题——**反转头到底值不值得开、cutoff/模式要不要调**。

---

## 0. 一句话口径

日报用「**实际 − 反事实**」归因（表内 `delta`），回答三个逐层递进的问题：

| 层 | 问题 | 支撑决策 |
|---|---|---|
| L1 判别力 | 反转头判"反转"判得准不准（分箱校准） | cutoff 是否要调、模型要不要重训 |
| L2 动作价值 | 按判定收紧/放宽 SL 后，相对**不动 SL** 多赚/少亏多少 | act 模式（真调 SL）去留、方向约束松紧 |
| L3 摩擦风险 | 有没有"帮倒忙"（killed 单）、频繁改单摩擦、尾部恶化 | 冷却 15min / 仅朝有利方向 / 台阶 0.5ATR 等纪律是否够 |

---

## 1. 现状盘点（2026-09-04 实测，全部 file:line 可查）

### 1.1 反转头运行时链路已完整就位

```
主号浮亏>0.6ATR(保本不触发,冷却15min,加深0.5ATR台阶重触发)
  → mt5_bridge._rev_request_scores (mt5_bridge.py:4493)
  → Redis hcm:ai:rev:req:{acct}:{ticket} (TTL 900s)
  → quality_scorer._rev_tick：300根M5窗口 → 22维特征 → 推理 → 校准
      (quality_scorer.py:1085 / :1142) → 写 hcm:ai:reversal:{acct}:{ticket} (TTL600s)
  → mt5_bridge._rev_apply_verdicts (mt5_bridge.py:4667)：
      score>cutoff → is_reversal=True
      mode=log（默认）→ 仅记日志不改仓 (mt5_bridge.py:4696-4701)
      mode=act → 反转:SL收紧至 现价∓0.15×ATR；回踩:SL放宽至 entry∓(时段系数×ATR)
                 幂等键 hcm:ai:rev:acted:* (mt5_bridge.py:4707)；仅朝有利方向 (4733/4735)
  → _rev_log_adjust 落 hcm_ai.reversal_attribution (mt5_bridge.py:4554, 仅 act 分支 :4759)
  → _rev_settle_closed 平仓后结算反事实 (mt5_bridge.py:4577)
```

### 1.2 模型与归因字段（v1 快照）

- 模型三件套：`tools/models/lgbm_reversal_v1.txt` + `calib_reversal_v1.pkl` + `reversal_v1_meta.json`（2026-09-02 16:19 训练）
- 判定规则：`percentile`，`score_pct=35` → **cutoff=0.4205**（校准后分数第 65 百分位）
- 测试段指标（meta.json 内置）：AUC=0.723 / precision=0.552 / recall=0.481（65 百分位档：n_trig=67, tp=37, fp=30）
- 22 维特征 = **8 持仓状态维**（pos_drawdown_atr / pos_bars_in_trade / pos_mfe_atr / pos_mae_atr / pos_sl_dist_atr / pos_tp_dist_atr / direction / session）+ **14 行情维**（adx/rsi/macd/di/er/bbw/donchian/close_mom/body/wick…）

### 1.3 归因表 schema（已建，`hcm_ai.reversal_attribution`，23 列，**0 行**）

关键列：`account_id/ticket/symbol/direction/entry_price`（身份）、`adj_ts/price_at_adj/old_sl/new_sl/atr`（调整现场）、`score/cutoff/is_reversal/mode/dd_atr`（判定）、`closed_ts/exit_price/realized_pnl/cf_exit_price/cf_pnl/delta/verdict`（结算）。

### 1.4 四个决定性现状（报表设计的前提）

1. **`ai.rev.*` 配置未下发**：Redis `hcm:config:v2` 中无任何 `ai.rev.*` 键 → `enabled` 走默认 `False` → **反转头当前整体关闭**（代码注释 2026-09-03 已追认 act 红线授权，但开关未下）。
2. **归因表 0 行**：无任何可展示的实盘/影子样本。
3. **采集缺口（本设计头号改造项）**：`_rev_log_adjust` 只在 **act 分支**被调用（mt5_bridge.py:4759）。`mode=log` 时 `_rev_apply_verdicts` 直接 `continue`（:4701）→ **shadow 期不产生任何归因行** → 若照现状先开 log 攒样本，日报将恒空，无法无风险评估价值。
4. **delta 是价格差口径，未乘手数**：`realized = (exit−entry)×sign`（mt5_bridge.py:4623），未含 `volume`。XAUUSD 1 手价格差 1.0 ≈ $100，不乘手数会**系统性低估/误导**跨仓汇总 → 日报 v0 用 R 归一（delta/atr），v1 需补 volume 列。

---

## 2. 价值贡献度量口径（报表内核）

### 2.1 单笔结算归因（已实现，mt5_bridge.py:4623-4653）

- `realized_pnl` = (实际平仓价 − entry) × 方向符号（平仓价取 DEAL_ENTRY_OUT 成交）
- `cf_pnl`（反事实）＝假设**未调 SL**：
  - 查 `adj_ts → 平仓时刻` 区间 M5 极值；BUY 且 `MIN(low) ≤ old_sl`（或 SELL 且 `MAX(high) ≥ old_sl`）→ 反事实在 **old_sl 止损**；
  - 未触及 → 反事实以实际平仓价离场（cf_pnl = realized）
- `delta = realized_pnl − cf_pnl`；`verdict = saved(>0) / killed(<0) / neutral(=0)`

> **口径局限（报表必须永久注记）**：
> ① 价格差口径，未乘手数、未扣点差/手续费；② M5 极值近似盘中触价（跳空/插针误差）；③ 反事实以「实际 deal_time」截断——若实际是收紧 SL 扫出，反事实中该仓应继续持有，此处被低估持有时间；④ 分批平仓只取末笔 entry=1 成交（mt5_bridge.py:4608-4611 循环覆盖）。

### 2.2 组贡献（日报聚合口径）

| 指标 | 定义 | 用途 |
|---|---|---|
| ΣΔ（价格差） | 当日已结算行 delta 求和 | 直觉总量（注记未乘手数） |
| ΣΔ_R | Σ(delta/atr)，R 归一、跨仓可比 | **主指标**，v0 推荐 |
| saved / killed / neutral 单数 | verdict 计数 | 动作质量分布 |
| 净胜率 | saved/(saved+killed) | act 模式去留判据之一 |
| 触发→动作→结算漏斗 | 见 §5 表二 | 链路健康与样本完备性 |

### 2.3 判别力口径（L1，独立于资金贡献）

对已结算行按 `score` 分箱（0.1 步长或十分位），统计每箱内「判定 is_reversal」的**实际反转率**。注意：act 行结局已被动作改变，只有 **log/shadow 行**能干净评估判别力 → **这也是必须补 log 落库的原因**（§4）。

---

## 3. 数据源与补强设计

| 层 | 现状 | 补强动作 | 风险 |
|---|---|---|---|
| 采集 | attribution 仅 act 分支落行 | **A1（必做）**：`_rev_apply_verdicts` log 分支（mt5_bridge.py:4696）同样调 `_rev_log_adjust`，`mode='log'`，`new_sl` 传**假设动作目标值**（不 MODIFY、幂等键仅记录） | 0（纯写入，不改仓） |
| 采集 | 部分"想动没动成"（不满足朝有利方向约束 4733/4735 而 continue）无痕 | **A2（随 A1）**：此类跳过也落一行 `mode='log_skip'` + 复用 `new_sl` 预估值，便于漏斗对账 | 0 |
| 采集 | delta 无手数 | **A3（v1）**：attribution 增列 `volume real`，`_rev_log_adjust` 从 pos.volume 带出；结算时已有关联 deal volume 回填 | 低（additive 迁移） |
| 结算 | 已覆盖（_rev_settle_closed 独立于 enabled 判断，:4675-4681） | 无需改 | — |

> A1 使 **shadow 期即可产出"假设动作"归因样本**：日报同时呈现「shadow 贡献（log，未真实改单）」与「act 贡献（真实改单）」两条线，前者是后者的无风险先行指标——这正是"零实盘风险先证伪"路线在报表侧的落点。

---

## 4. 新增日聚合表 DDL（建议 `hcm_ai.rev_daily_report`）

> 对齐既有 `hcm_ai.daily_kpi` 的 upsert-on-trade_date 幂等模式（scheduler.py:4515 `_aggregate_daily_kpi`）。

```sql
CREATE TABLE IF NOT EXISTS hcm_ai.rev_daily_report (
  stat_date      date PRIMARY KEY,
  -- ① 漏斗（当日新增 adj 行）
  n_trigger_pos  integer NOT NULL DEFAULT 0,   -- 触发候选仓次（浮亏>0.6ATR）
  n_requests     integer NOT NULL DEFAULT 0,   -- 评分请求
  n_scored       integer NOT NULL DEFAULT 0,   -- 有结论
  n_rev_call     integer NOT NULL DEFAULT 0,   -- 判定=反转
  n_pull_call    integer NOT NULL DEFAULT 0,   -- 判定=回踩
  -- ② 动作
  n_act          integer NOT NULL DEFAULT 0,   -- act 模式实改
  n_shadow       integer NOT NULL DEFAULT 0,   -- log 模式假设动作
  -- ③ 当日结算（closed_ts 落当日）
  n_settled      integer NOT NULL DEFAULT 0,
  n_saved        integer NOT NULL DEFAULT 0,
  n_killed       integer NOT NULL DEFAULT 0,
  n_neutral      integer NOT NULL DEFAULT 0,
  sum_delta      double precision NOT NULL DEFAULT 0,   -- 价格差口径
  sum_delta_r    double precision NOT NULL DEFAULT 0,   -- R 归一（主指标）
  avg_delta_r    double precision,                      -- 单笔平均贡献(R)
  -- ④ 明细与分箱（紧凑存储，前端解析）
  detail         jsonb NOT NULL DEFAULT '{}',   -- 见 §5 表三/表四结构
  updated_at     timestamptz NOT NULL DEFAULT now(),
  CONSTRAINT rev_daily_consistency CHECK (n_saved + n_killed + n_neutral <= n_settled)
);
CREATE INDEX IF NOT EXISTS idx_rev_attr_adj  ON hcm_ai.reversal_attribution(adj_ts);
CREATE INDEX IF NOT EXISTS idx_rev_attr_settle ON hcm_ai.reversal_attribution(closed_ts)
  WHERE closed_ts IS NOT NULL;
```

聚合 SQL 骨架（对齐 `_aggregate_daily_kpi` 的只读聚合 + upsert）：

```sql
-- 当日结算（主视图）
SELECT (closed_ts AT TIME ZONE 'UTC')::date  AS d,
       COUNT(*)                              AS n_settled,
       COUNT(*) FILTER (WHERE verdict='saved')    AS n_saved,
       COUNT(*) FILTER (WHERE verdict='killed')   AS n_killed,
       COUNT(*) FILTER (WHERE verdict='neutral')  AS n_neutral,
       COALESCE(SUM(delta),0)                AS sum_delta,
       COALESCE(SUM(delta/atr),0)            AS sum_delta_r
FROM hcm_ai.reversal_attribution
WHERE closed_ts IS NOT NULL AND atr > 0
GROUP BY 1;
```

---

## 5. 报表版面设计（每日 1 版）

### 表一 · 头部 KPI 卡（当日 + 7 日滚动）

| KPI | 口径 | 置信门槛 |
|---|---|---|
| 净贡献 ΣΔ_R（当日） | 已结算 saved−killed 的 R 合计 | 结算样本 < 10 → 显示「样本不足(观测中)」 |
| 净贡献 ΣΔ_R（7 日） | 滚动 7 日 | 同上 |
| 动作胜率 saved/(saved+killed) | 当日 | n<5 灰置 |
| 待结算笔数 | closed_ts IS NULL 且已离场未回填 | 恒显（补结算健康度） |
| 累计 saved−killed | 全部历史 | — |

### 表二 · 触发→动作→结算漏斗（当日）

```
浮亏>0.6ATR 候选   n_trigger_pos
   ├─ 评分请求      n_requests
   │   ├─ 有结论    n_scored → 判定:反转 n_rev_call / 回踩 n_pull_call
   │   └─ 无结论    （模型关/超窗/异常 → 失败安全）
   └─ 未触发        （保本/冷却中/未加深台阶）
实改(act) n_act ──┐
假设(log) n_shadow ┤→ 已结算 n_settled → saved / killed / neutral
```

### 表三 · 分箱校准（判别力 L1，仅 log/shadow 行）

`detail` 内嵌 `[{bucket:"0.30-0.40", n, n_rev_call, rev_rate, saved_rate}, …]`，每箱 n<5 不渲染结论列。

### 表四 · 切片 × 模式（L2/L3）

`detail` 内嵌数组，切 4 维：**判定**(反转/回踩) × **模式**(log/act) × **时段**(asia/europe/us) × **浮亏深度档**(0.6–1.1 / 1.1–1.6 / >1.6 ATR)。每格：n / ΣΔ_R / saved 率。深度档直接反映"AI 在刀口多深时出手"的价值密度。

### 表五 · 明细抽屉（最近 20 笔已结算）

ticket / 判定日 / 方向 / dd_atr / score / verdict / old_sl→new_sl / realized / cf / delta / Δ_R。

### 版尾 · 口径注记（固定）

§2.1 五条局限 + "delta 未乘手数（v0 以 R 归一替代）" + 数据截止时刻。

---

## 6. 价值贡献的三种语义（防误读硬规则）

1. **Shadow 贡献（log 行 ΣΔ_R）**＝假设启用时能省的——先于授权出现，是决策先行指标；
2. **Act 贡献（act 行 ΣΔ_R）**＝真实已兑现的——红线授权后的最终裁判；
3. **判别力（分箱 rev_rate）**＝解释"为什么 1/2 会成立或不成立"，独立于资金口径。
> 日报任何位置不得把三者混标；KPI 标题必须带模式标签（shadow/act）。

---

## 7. 调度与前端

### 7.1 聚合节拍（对齐 daily_kpi 三拍）

在 scheduler 每日 KPI 循环（scheduler.py:4490-4513）同处新增 `_aggregate_rev_daily`（或独立协程同节拍）：
1. 首次回填近 30 天（对 attribution 空表天然安全）；
2. 每日 `now ≥ 昨日+1天 00:05` 后补算昨日（upsert，幂等）；
3. 当天实时 upsert（供前端当日进度）。
> 只读聚合 + upsert 日报表，零生产写路径，与 `_aggregate_daily_kpi` 纪律一致。

### 7.2 前端（只读展示）

- 路由/页：`hcm-web/web/api/ai_report.py` 仿 `/api/v1/ai/report/monitor`（:300）新增 `/api/v1/ai/report/reversal`；页面挂 `/signal-tower/model-monitor` 同体系（新增 Tab「反转头日报」或独立卡片，遵循既有深色令牌 #0d0d14/#111118/#161622/#2a2a3a，涨红跌绿不适用——本表无行情涨跌色，saved 用 ok 绿 #22c55e / killed 用 block 红 #ef4444）。
- 渲染：表一~表五静态表格 + 7 日 ΣΔ_R 迷你趋势（ECharts 折线），无交互写操作。

---

## 8. 上线路线（四步，每步可独立验收、可回退）

| Step | 内容 | 改什么 | 风险/验收 |
|---|---|---|---|
| **A** | 采集补强：log/log_skip 分支也落 attribution（A1/A2） | mt5_bridge.py `_rev_apply_verdicts` log 分支 | 0 交易风险；验收＝log 模式下 attribution 出现 mode='log' 行 |
| **B** | rev_daily_report 建表 + 聚合脚本/协程 + Redis/接口 | deploy/migrations 新 SQL + scheduler.py 或独立 tools 脚本 | 只读；验收＝空表安全、补算幂等 |
| **C** | 前端 Tab（只读） | hcm-web api/ai_report.py + model-monitor 页组件 | 只读；验收＝空态占位「观测中」 |
| **D** | 启用 `ai.rev.enabled=true`（mode 仍 log）→ 攒 shadow 样本 → 报表出「shadow 贡献」→ 达标后再议 act | 配置下发（用户授权） | 授权红线；验收＝shadow 归因样本 ≥ 30 |

> 关键决策点：**D 的"达标"标准建议预设**（如 shadow 结算 ≥30 笔且 saved/(saved+killed) ≥ 55%），避免"看报表再拍脑袋"。

---

## 9. 风险与红线

1. 本设计全部改动 = 新增采集写入 + 只读聚合 + 只读展示；**不改** reversal_attribution 结算口径、不改 act 判定逻辑、不碰 quality 主链路。
2. 小样本纪律：任何 n<10 的格子只允许出现「观测中/样本不足」，**禁止据此下"反转头无价值/有价值"结论**（用户硬证据红线）。
3. delta 未乘手数在 v0 必须永久注记，禁止把价格差当美元金额汇报。
4. log_skip 行（想动没动成）需与 act 行区分标签，防止污染"动作价值"统计。

---

## 10. 待你拍板

1. **A1 采集补强是否授权**（改 mt5_bridge.py 一个分支，纯写入、零交易影响）——这是日报能在 shadow 期就有数据的前提；
2. 日报聚合落 **PG 新表**（推荐，可回溯）还是仅 Redis 键（仿 monitor report latest/history）；
3. 前端挂 model-monitor 加 **Tab**（推荐）还是独立页；
4. Step D 的"达标"预设标准（建议：shadow 结算 ≥30 笔且净胜率 ≥55%）是否认可。
