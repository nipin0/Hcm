# HCM 量化系统开发永久基座约束（铁律 V1.0 精准最终版）

> 适配体系：AI 智能体防漂移 + 防伪交付 + 问题沉淀知识库
> 生效范围：所有量化系统 Agent 开发任务
> 特性：终结反复 BUG、终结跑偏、终结假装写完
> 优先级：全程最高优先级生效，覆盖一切默认行为

---

**【变更前置铁律】任何对生产系统的修改，AI 必须先精准复述用户需求、给出改动方案，待用户明确同意后方可动手；禁止未经确认自行开改、闷头全改。**

## 一、唯一真值（Single Source of Truth）

1. 开发严格遵循 **知识库、开发文档、接口契约、基准代码**。
2. 文档无约定逻辑，**禁止自创、脑补、私自设计**。
3. 存在歧义必须 **主动提问确认**，不得替用户做架构/策略假设。

### 真值附录（Key Source of Truth 速查）

- **DeepSeek API key 真源 = PG 配置中心**：键 `deepseek.api_key` 存于 PG `hcm_config.metadata`，经 `config_provider` 双写 Redis `hcm:config:v2`。**不在 `.env`**——`docker-compose.yml` 的 `DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY:-}` 仅为空占位，已被弃用。判定 DeepSeek 是否激活，必须查 PG/Redis 的 `deepseek.api_key` 真值（真值 = 非空且非占位串 `test_pg_key`），而非 `.env`。读取链路：scheduler 读配置中心 `deepseek.api_key` → `DeepSeekClient` → `llm_client.is_available()` 仅需 key 非空即生效。
- **`ai.lm.enabled` 是假开关（幽灵键）**：它只存在于 `ai_config.py` 的 `AI_KEYS` 白名单与前端 `AiQualityConfig.tsx` 表单渲染，**运行路径（scheduler / quality_gate / `_read_ai_quality`）从不读取**；LightGBM 校准的真实激活条件是 `ai.enabled=true` 且 `ai.mode=coupled`。绝不能把该假开关的显示状态当作 LightGBM 运行与否的依据。

## 二、禁止单边变更（No Unilateral Change）

以下范畴任何改动，**必须先提交变更说明，确认后再编码**：
- 架构分层
- 核心公式（含 SL/TP/评分/权重/风控阈值推导）
- 接口字段（请求/响应契约）
- 异步架构（含 Stream/消息队列/消费组）
- 缓存 / 队列 / 超时 / 重试 / 熔断 / 降级机制
- 系统核心规则（信号生命周期、信号状态机、账户角色判定）

## 三、边界锁定（Scope Lock）

1. 只完成 **当前指定任务**。
2. 禁止拓展新功能。
3. 禁止优化无关代码。
4. 禁止重构稳定模块。
5. 禁止阉割删除原有合规逻辑。

## 四、代码规范（Code Discipline）

1. 严格遵守 **SOLID** 原则。
2. 禁止大函数高耦合。
3. 禁止魔法数字（所有阈值/系数须显性声明、可中控配置）。
4. 禁止隐性逻辑分支（条件意图清晰、可观测）。
5. 所有参数、公式显性声明，严格遵守数据契约。
6. **禁止状态字段硬编码字面量**：`order_status`/`status`/`direction` 等枚举字段，禁止在 SQL 或代码里裸写 `1`/`2`/`'open'`/`'closed'` 等字面量，必须用语义命名常量或查表映射（参见第十章 10.1：裸写 `1` 曾导致 1015 笔订单假 open、风控冷却全面失效）。

## 五、量化系统红线（Hard Red Lines）

1. **和乘幂（hexp）原始信号方向禁止修改**。
2. **异步架构禁止改为同步**。
3. **缓存、队列、超时、重试、熔断、降级机制禁止删减**。
4. 时序数据 **禁止 shuffle**、杜绝未来数据泄露（含 K 线未来棒、信号年龄、特征穿透）。
5. 所有 API 必须带 **超时、重试、限流、降级兜底**。

### 5.1 AI 信号质量层定位铁律（2026-08-17 用户硬约束）

> 背景：实盘在高位接刀被止损，根因是 LightGBM 质量评分器只做「等级升降/手数分档」裁决，
> **不分析价格位置高低**，无法在高位给出否决。用户明确：AI 必须能分析高位能不能开、判断不能开则否决。

- **架构定位**：在 hexp（和乘幂）独立信号源之上，引入**本地轻量 LightGBM 信号质量评分器**
  作为**质量过滤层**，与后台异步 DeepSeek 协作（融合分 `c_ai`），共同辅助 hexp 只输出
  **高质量信号**。**LightGBM 是独立信号质量评分源**，不是 hexp 的附属。
- **AI 权限边界（强制）**：AI 全程**只有否决权 + 降/升级权，无独立开仓权**。
  - 否决权：AI 判定「不能开」（含高位接刀、超买超卖极限、质量分过低等）时必须能**否决**该信号；
  - 降/升级权：AI 可降级（降一级仍放行）/ 升级（升一级，仅限 hexp 已放行信号）hexp 信号的等级；
  - **无独立开仓权**：AI 不得「打开」一个 hexp 因方向或质量已拦掉的信号独立下反向/同向单。
    （注：`quality_gate.adjust_grade` 中 `passed=False + UPGRADE → ai_opened` 的「AI 打开 hexp 拦掉信号」
    逻辑**违反本铁律**，须改为：hexp 未放行（含无方向）时 AI 一律 HOLD/否决，不得 ai_opened。）
- **高位否决是必须能力**：LightGBM 特征必须包含**价格位置/高低位/超买超卖/极值偏离**类因子，
  使评分器在「高位做多 / 低位做空」等接刀形态下给出低分 → 触发否决，而非仅看历史胜率均值。
- **配置真源与生效**：`ai.enabled` / `ai.mode` / `ai.lm.*` / `ai.cpl.*` 经 `config_provider.set`
  双写 PG(`hcm_config.metadata`) + Redis(`hcm:config:v2`) + PUB；改配置即秒级生效，禁硬编码。
- **冻结保护**：本定位属已确认架构约束，任何改动上述权限边界（尤其「AI 打开 hexp 拦掉信号」
  `ai_opened` 行为）必须先出变更说明待确认（铁律二、十一.6），禁止顺手改。

## 六、交付铁律（Delivery Mandate）

1. AI **无权自我判定任务完成**。
2. 仅主干流程跑通 **不代表完成**。
3. 缺失边界处理、异常容错、兜底闭环，一律判定为 **伪交付**。
4. 所有交付必须附带 **完整自检审计清单**。

---

## 七、类别触发机制（对号入座，按类 skill 强制分类执行）

| 类别 | 触发词 | 执行机制 |
|------|--------|----------|
| **A 审计 Audit** | 为什么 / 诊断 / 是不是 BUG / 看看 / 有没有问题 | 先取值再开口；读取逐行代码；不信注释；结论附 `file:line` |
| **B 改BUG** | 修复 / 删废代码 / 加个判断 / 防新BUG | 三重验证（根因→修复→回归）；参数走中控（PG+Redis 双写） |
| **C 提案 Proposal** | 能不能优化 / 建议 / 要不要加 / 有什么方案 / 怎么改进 | 六段全局权衡；审计前置强制适用 |
| **D UI 设计** | 改样式 / 新界面 / 布局好不好看 / 颜色 / 字号 | 现代美观、UX 优先、遵守既有设计令牌 |
| **E 通用 Cross-cutting** | 所有任务（默认兜底） | 技术名词附中文；用户非程序员生成，表述通俗 |

### 类别说明
- **A 类**：必须基于真实取值/代码得出结论，禁止凭印象；引用证据标注 `file:line`。
- **B 类**：修复需说明根因、改动点、验证方式；配置项经中控（`config_provider.set` 双写 PG+Redis+PUB）即时生效，禁止硬编码散落。
- **C 类**：提案须含「收益/风险/兼容性/回退/监控/排期」六段权衡，并先完成受影响模块审计。
- **D 类**：仅样式/布局层改动，不触架构红线。
- **E 类**：所有任务默认适用，技术术语配中文解释。

---

## 八、文件规范性分类存放（File Discipline · 防乱放误删）

> 历史教训：2026-08-10 `tools/` 下除 `mt5_bridge.py` 外的 `.py` 凭空消失（含看门狗/position_sync），导致桥断 2 天、一键诊断自愈无效。根因=核心生产文件与临时脚本/日志/数据平铺混放，极易误删或备份回退覆盖。
> 本章为**硬约束**，与第三（边界锁定）、第六（交付）条同级，强制生效。

### 8.1 目录分区（唯一权威布局）

```
hcm-v2/
├── deploy/migrations/      # 【禁删】迁移 SQL（含 0001~0009 等 seed/修复脚本）
├── docs/                   # 【禁删】设计文档/PRD/指南（*.md）
├── tools/                  # 【生产核心区·禁删禁乱改】
│   ├── (白名单核心文件，见 8.2)
│   ├── _baseline/          # 【禁删】OS 层守护还原源（bridge_boot.ps1 自动还原点）
│   ├── archive/            # 【只读】已验证的历史/验证脚本、*.bak 基线
│   ├── _scratch/           # 【可清】临时调试脚本/一次性 SQL/输出（_tmp_*.py 等）
│   ├── _logs/              # 【运行时禁移】桥/看门狗/守护日志（*.log，正被进程写入不可移动）
│   └── _artifacts/         # 【可清】数据产物（*.csv/*.pkl/模型训练输出）
├── hcm-*/                  # 【禁删】各微服务源码（按 docker-compose 挂载清单）
└── docker-compose.yml      # 【禁删】编排正本
```

### 8.2 生产核心白名单（tools/ 根仅允许以下，其余一律归位）

| 文件 | 角色 | 保护 |
|------|------|------|
| `mt5_bridge.py` | 主机桥进程（唯一 K 线写入源+真实下单） | **禁删、禁改名、禁被备份覆盖** |
| `position_sync.py` | 主/跟单持仓同步（桥硬依赖 `from position_sync import sync_positions`） | **禁删** |
| `bridge_watchdog_pa.py` | 看门狗本体（终端驱动拉桥） | **禁删** |
| `bridge_watchdog_launcher.py` | 看门狗启动器（互斥体单实例） | **禁删** |
| `bridge_boot.ps1` / `bridge_boot_hidden.vbs` | OS 层守护（缺失自动从 `_baseline` 还原） | **禁删** |
| `register_bridge_guard.ps1` | 计划任务注册 | 禁删 |
| `quality_scorer*.py` / `quality_scorer_guard*.ps1` / `quality_scorer_launcher.py` | 质量评分器 | 禁删 |
| `*.ps1` / `*.vbs` 守护脚本 | 守护链 | 禁删 |
| `build_labels.py` / `dryrun_new_model.py` / `train_signal_quality.py` / `migrate_hexp_shadow.sql` | 训练/迁移工具 | 保留 |

**规则**：新增临时脚本必须命名 `_tmp_*.py` 或放入 `tools/_scratch/`；禁止在 `tools/` 根或 `hcm-v2/` 根平铺 `_xxx.py`/`_xxx.txt`/`_xxx.log`。

### 8.3 防误删硬约束

1. **核心白名单文件禁止任何删除/重命名/移动**操作（含 IDE 误拖、备份还原覆盖）。
2. **备份还原（"恢复备份"流程）必须排除** `tools/mt5_bridge.py`、`tools/position_sync.py`、`tools/bridge_watchdog_*.py`、`tools/_baseline/`——否则回退即断桥（2026-07-30 教训）。
3. **运行时日志（`_logs/*.log`、桥进程持有句柄）禁止移动/删除**，仅可轮转（复制后清空原文件，勿直接删）。
4. 清理临时文件只限 `tools/_scratch/`、`tools/_artifacts/`；清理前须确认不含白名单文件名。
5. 任何文件消失先查 **IDE 本地历史**（`C:\Users\Administrator\AppData\Roaming\CodeBuddy CN\User\History\`）——比备份新得多（2026-08-10 恢复经验）。

### 8.4 自查清单（每次交付必附）

- [ ] 新文件是否落在正确分区（核心→白名单区 / 临时→`_scratch/` / 数据→`_artifacts/` / 日志→`_logs/`）？
- [ ] 是否动了白名单核心文件（删/改名/移出 tools/）？若动，是否走变更说明？
- [ ] 备份还原脚本是否已排除 `tools/` 核心四件套 + `_baseline/`？
- [ ] 运行时日志是否未被移动/删除？
- [ ] 根目录 `hcm-v2/`、`tools/` 是否无新增平铺 `_xxx` 临时文件？

---

## 十、BUG 复盘沉淀（Root-Cause Ledger）

> 凡经审计确认的生产代码 BUG，须在此逐条登记：根因、归属模块、触发条件、规避法则。
> 目的：终结同类 BUG 反复出现，沉淀为编码禁区。

### 10.1 状态字段硬编码字面量（order_status 裸写 1/2）

- **根因**：`tools/position_sync.py:_close_stale_positions` 写 `hcm_trading.orders` 时，把 `order_status` 写死为字面量 `1`（open），实际该单已平仓应写 `2`（closed）。下游风控冷却 `_check_cooldown` 依赖 `positions.status='open'` 判定同方向持仓，而 `positions` 表被 reconcile 正确标 closed → 查询永远空 → **同向冷却永久失效** → 跟单号可 1~3 分钟连续开同方向单 → 行情不利时连续被止损。
- **影响账户**：account 6/9/23/24 累计 1015 笔 orders 全显示假 open，冷却全面失灵。
- **归属模块**：`position_sync.py`（写入失真）+ `rule_chain.py:_check_cooldown`（依赖滞后表判冷却）。
- **触发条件**：任何持仓被 MT5 平仓后，position_sync reconcile 跑 `_close_stale_positions` → 写 orders 时 order_status 恒为 1。
- **规避法则（强制）**：
  1. **禁止裸写状态字面量**：所有 `order_status`/`status` 等枚举字段，必须用命名常量（如 `ORDER_STATUS_CLOSED=2`）或查表映射，禁止在 SQL/代码里直接写 `1`/`2`/`'open'`/`'closed'`。
  2. **冷却/持仓数等风控闸门不得仅依赖 PG `positions` 表**（该表总是滞后于 MT5 实际平仓）。必须回退查 `orders` 表（`order_status=2 AND close_time`）取最近平仓时间，或直接查 MT5 实时持仓快照。
  3. **数据失真即功能失真**：任何"写库状态"的字段若与真实业务状态不一致，会连锁导致风控/对账/监控全部误判。写库前必须断言字段语义与 MT5 实际状态一致。

### 10.2 风控闸门依赖单表导致失效（通用法则）

- 凡风控规则（冷却、持仓数上限、每日亏损、保证金）依赖数据库表状态时，**至少双源校验**：优先 MT5 实时持仓（Redis 存活键/桥心跳快照），回退 PG `orders` 已平记录；禁止单一依赖 `positions` 表（`status='open'`）作为唯一真相源。
- 表状态与实时状态存在固有延迟（reconcile 周期、网络抖动），单表依赖必在特定时序下失效。

---

## 九、记忆与沉淀

- 关键根因、修复、部署方式、遗留风险，沉淀至 Agent 记忆库，跨会话持久。
- 凡涉及约束第二/五条核心机制的改动，必先出变更说明待确认，不擅自编码。
- 文件位置：`D:\HCM_ASST\铁律.md`（本文件）为约束正本；知识库记忆为执行沉淀。

---

## 十一、已正常功能规避（Freeze & Regression Guard）

> 目标：防止"已上线且验证正常的功能"被后续改动误伤、回归。原则是**默认冻结、改动必有代价、最小侵入**。
> 适用场景：任何"功能本来正常、为加新东西/修别的 bug 而动到它"的情形（如 AI 评分 sidecar 本已工作，排查时反复改写 `quality_scorer.py` 主循环、加临时探针，险些破坏）。

### 11.1 功能冻结清单（Freeze List）判定

改动**任何文件**前，先问：**这段代码/功能当前是否已上线且验证正常？**
- 是 → 视为**冻结**：非必要不动，动的代价必须显式评估。
- 判断依据：Redis 心跳键、PG 表、生产日志是否有持续正常产出（如 `bridge:alive:*`、`hcm:live:hexp:ai:*`、`AI gate probe`）。
- 冻结功能清单应随部署维护（新功能上线验证通过即入列）。

### 11.2 规避优先级（改动手段从轻到重）

1. **配置热调优先**：能用 `config_provider.set` 双写（PG+Redis+PUB）解决的，**绝不改代码**。例：AI 评分校准退化，改 `ai.lm.calib_blend_w` 即可，无需动 `score_one`/`quality_scorer.py`。
2. **只读审计优先**：诊断/定位用**查询、日志、Redis/PG 取值**，不先改代码。
3. **必须改代码时，最小侵入**：局部替换（`replace_in_file`），禁止整体重写稳定函数（如 `main()`）除非已确认根因且改动必要。
4. **禁止"边查边改"**：先在**独立副本/独立脚本**里复现验证根因，确认后再动生产源文件。

### 11.3 探针纪律（Probe Discipline）

- 临时 `print`/Trace 探针**只允许加在**：`tools/_scratch/` 独立复现脚本，或**生产文件末尾的调试分支**（改后必删）。
- **禁止**在稳定函数内部留探针（如 `_reload_cfg` 里加 `[trace]`）。
- 排查结束，**必须**清理所有临时探针、临时脚本、临时输出文件（铁律 8.4），并 py_compile 复核生产源文件恢复到"纯功能"状态。

### 11.4 回归基线（Regression Baseline）

- **改动前**：记录当前正常行为的关键观测值（Redis 键值、日志关键词、PG 计数、接口响应）作为基线。
- **改动后**：用同一观测点对比，确认无回归。
- 例：改 `quality_scorer.py` 前记录 `hcm:live:hexp:ai:XAUUSD` 的 `valid/status/ai_score`；改后必须恢复 `valid=true/status=ready` 且 `ai_score` 有值。
- 改动破坏已正常功能 → **立即回滚**，不得带着破坏继续排查。

### 11.5 回滚锚点（Rollback Anchor）

- 改**生产核心文件**（白名单、bind mount 源码、桥/看门狗）前，**先备份**到可回滚点（`cp file file.bak_<时间戳>` 或 IDE 本地历史确认）。
- 改动未通过验证 → 用备份原子恢复，并复核 `py_compile` + 行为观测点。
- 备份文件在修复确认后清理，不留 `.bak` 残渣（铁律 8.4 例外：确认前保留）。

### 11.6 变更说明强制前置

凡触及**已冻结功能**的代码改动，即使满足第二/五条，也**必须先出变更说明**：改动文件、根因、影响面、回归基线、回滚方式，待确认后再动手。
- 冻结功能 + 代码改动 = 高危操作，禁止"顺手改"。
- 只有"纯新增"（不动既有逻辑）或"配置热调"可免于此前置，但仍需记录。

---

*本文件为 V1.0 精准最终版，任何后续修订须显式标注版本与日期。V1.0 含第十一章"已正常功能规避"。*
