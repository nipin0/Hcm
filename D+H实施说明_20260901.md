# D+H 实施说明（纯可观测性改动）

日期：2026-09-01　性质：**代码已落盘，未部署**（重启需另行授权）
回滚锚：`*.pre_dh_20260901_171229`（4 个文件）

---

## 改动清单

| 文件 | 改动 | 性质 |
|---|---|---|
| `hcm-signal-tower/signal_tower/scoring_engine.py` | ScoreResult 增 `grade_vetoed: bool = False` / `grade_veto_by: str = ""` | 纯新增字段 |
| `hcm-signal-tower/signal_tower/hexp_engine.py` | ① live 快照发布前新增 §10.5 否决检测；② `hcm:live:hexp:{SYM}` 增 `grade_vetoed`/`grade_veto_by` | 纯观测 |
| `hcm-signal-tower/signal_tower/scheduler.py` | 落库 `indicator_values._hexp.grade_vetoed` / `grade_veto_by` | 纯观测 |
| `tools/position_sync.py` | 新增 `_infer_close_reason()` + `_CLOSE_REASON_BY_DEAL`；`close_reason` 由硬编码 `sync_reconcile` 改为真实归因 | 数据质量 |

---

## D：grade 与安全护栏同步

**问题**：grade 在 `hexp_engine.py:1578` 先评出，而 A11–A16 安全护栏全部跑在其后，只改 `direction`/`threshold_passed`、**不回改 grade**（`2458` 兜底仅在 `_grade_ok=False` 时触发）。→ 面板与 `signals.indicator_values._hexp.grade` 显示 A/S，但该信号实为 NO_TRADE。

**做法**：**加观测标记，不改 grade 语义**（下游 min_grade 闸门与既有统计都依赖原语义，改语义有回归风险）。

判据：`_grade_ok and (not passed or direction=="NO_TRADE" or not threshold_passed)`

归因优先级：`momentum_flip` → `momentum_drain` → `range_hurst` → `cycle_pos` → `extreme_reversal` → `zone` → `extreme_guard` → `fallback_reason` → `unknown`

> 复用了已有的 `momentum_flip_blocked`(scoring_engine:270)、`momentum_drain_blocked`(269)、`range_hurst_blocked`(271)、`cycle_pos_blocked`(223)、`extreme_reversal_blocked`(200)、`zone_blocked`(261)，未新增护栏标记。

**验证**：抽取源文件真实代码块 exec 测试 **10/10 通过**。关键边界：
- `_grade_ok=False`（grade 未达标）→ **不算否决**，不误报
- 正常通过 → 不标记
- 6 类护栏 + fallback + unknown 归因全部正确

---

## H：平仓归因（close_reason 语义化）

**问题**：`tools/position_sync.py:861` 把 `close_reason` **硬编码** `'sync_reconcile'`，无法区分 SL/TP/保本/手动/强平（前端 `hcm-web/web/api/ai_report.py:189` 早已标注此缺陷）。

**做法**：回查 MT5 成交历史 `history_deals_get(position=ticket)`，取离场 deal（`entry==1`）的 `reason`：

| deal.reason | 写入值 | 含义 |
|---|---|---|
| 0 / 1 / 2 | `manual` | 客户端 / 手机 / 网页手动平仓 |
| 3 | `expert` | EA 程序平仓（移动止损、强制平仓等） |
| 4 | `sl` | 止损 |
| 4 且成交价≈开仓价 | `be` | **保本**（移动止损已推至开仓价） |
| 5 | `tp` | 止盈 |
| 6 | `stop_out` | 保证金不足强平 |
| 其他/失败 | `sync_reconcile` | 安全回退，保持原行为 |

BE 容差：`max(0.05, |开仓价| × 1e-4)`，对 XAUUSD（约 4500）约 0.45 美元。

**验证**：py_compile 通过；假 MT5 对象功能测试 **15/15 通过**，含全部异常路径（无离场 deal / 空历史 / 返回 None / mt5 缺失 / MT5 异常 / 未知 reason）均安全回退，**绝不阻断对账主流程**。字段读取兼容 namedtuple 与 dict 两种形态（首版只兼容前者，已修）。

**保守取舍**：`close_price` 仍用最后同步价，未改成成交价——仅换 `close_reason`，改动面最小化。

---

## ⚠️ H2 延后：落 `hcm_trade.closed_positions` 有 double-count 风险

`rule_chain.py:984-994` 的 `_get_daily_loss` 用 `UNION ALL` 同时读两张表：

```sql
SELECT profit AS pnl FROM hcm_trading.orders WHERE ... 
UNION ALL
SELECT realized_pnl AS pnl FROM hcm_trade.closed_positions WHERE ...
```

代码注释（`rule_chain.py:977-983`）已明写：

> closed_positions 当前为空，UNION ALL 不产生重复计数；**若将来启用归档写入，须复核两表是否会 double count 同一笔平仓**

**后果**：一旦开始写 `closed_positions`，同一笔平仓被计两次 → 当日亏损翻倍 → 可能触发 `risk_max_daily_loss` 熔断 → **掐死全盘下单**。

**正确做法**（需单独授权）：必须与 rule_chain 去重改造配对，并按序部署——
1. 先改 `rule_chain._get_daily_loss` 去掉 closed_positions 分支（该表当前为空，此改动**当下零行为变化**）+ 重启 risk-engine
2. 再启用归档写入 + 重启宿主桥

顺序颠倒会在窗口期内产生重复计数。

---

## 部署状态

| 组件 | 生效条件 | 当前 |
|---|---|---|
| D（grade 同步） | 重启 **signal-tower 容器**（docker） | 未重启 |
| H（平仓归因） | 重启 **MT5 桥**（宿主 Windows 进程，非 docker） | 未重启 |

重启属生产动作，按红线需显式授权，**本次未执行**。
