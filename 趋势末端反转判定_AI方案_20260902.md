# 趋势末端「反转 vs 回踩」AI 判定方案

> 生成时间：2026-09-02 | 性质：**只读侦察 + 方案分析**，本轮未改动任何生产代码/配置
> 证据口径：全部 `file:line` 引用 + PG 实测 SQL 结果，不含推测（推测处已显式标注）

---

## 0. 需求复述（请确认我理解无误）

| # | 你的原话 | 我的理解 | 待确认 |
|---|---------|---------|--------|
| 1 | 趋势末端行情反转、持仓浮亏时 | 触发条件 = ①持仓处于浮亏 ②市场处于「趋势末端/衰竭」态 | 浮亏阈值按 **R 倍数** 还是 **ATR 倍数**？ |
| 2 | 及时调用 AI 模型分析，是反转还是回踩 | 输出二分类 + 概率 | 是否需要三分类（反转/回踩/横盘）？ |
| 3 | 反转概率 > 40% → 缩小 SL，小亏平仓 | AI 判定为反转 → 把 SL 收紧到接近现价，让单子以小亏离场 | 收紧到多少？（建议 0.5×ATR） |
| 4 | 属于回踩 → 保 SL，放大到平仓配置的系数设定 SL 键值 | 判定为回踩 → 保持 SL 不收紧，并把 SL 距离**放大**到 `close.<session>.trailing_stop_distance` 配置的 ATR 系数 | **「平仓配置的系数」= `close.<session>.trailing_stop_distance`（asia 1.5 / europe 2.0 / us 2.0）** 是否是这个意思？ |
| 5 | 用 LightGBM 还是 DeepSeek 单独实现 | 选型分析 | 见 §4 |

---

## 1. 结论速览

**推荐：DeepSeek 先上（Shadow 模式）+ LightGBM 后补（离线回放训练），分三阶段落地。**

| 维度 | 结论 |
|------|------|
| **立即能用的** | DeepSeek（改 prompt + 输出字段 + 触发条件即可，零训练） |
| **长期该有的** | LightGBM 第四头（reversal head），但需要新建标签 + 持仓状态特征 + 离线回放管线 |
| **现在最该做的** | **离线回放验证**（阶段 0）——用现有 2008 笔已平持仓 + M5/M1 K 线，零成本验证「反转就缩 SL」这条规则历史上到底是赚是亏 |
| **最大风险** | 该逻辑让 **AI 首次获得主动平仓权**，与现有铁律「AI 仅持否决/降级/升级权，无独立开仓权」冲突；且会直接冲击现有 81%~98% 的保本率结构 |

---

## 2. 现状侦察（只读，硬证据）

### 2.1 AI 判定侧：已有能力

#### （A）DeepSeek 链路 —— `ai_async_client.py`（485 行）

| 能力 | 位置 | 现状 |
|------|------|------|
| 固定 3 输出 | `:4-6` | `fake_prob`（信号为真概率 0-1）、`ai_sl_coeff`（自适应止损系数 0.8~1.5×ATR）、`continuity_score`（趋势延续分 0-100，**设计用途即"持仓调仓依据"**） |
| prompt 组装 | `:39 build_prompt()` | 输入是 **HEXP 信号快照**（hp/方向/grade/H1 确认态/行情模式/因子冲突/六维评分卡/ATR/M1-MM/外部因子/LightGBM 25 维特征） |
| 输出解析 + 钳制 | `:144 parse_output()` | 值域钳制 + 越界告警（`:213-228`） |
| 超时降级 | `:186 produce_async_outputs()` | `asyncio.wait_for` 调用级超时（默认 `ai.ds.timeout_sec=15`），失败返回 `FALLBACK`（`:27`） |
| 规则触发循环 | `:365 run_loop()` | 仅当 Redis `ai:ds:trigger:{symbol}` 存在才调用，**异步不阻塞实时决策** |
| 落库 | `:333 _persist_ds()` | `hcm_ai.ds_output` + `hcm_ai.runtime_event` |

> ⚠️ **关键：`continuity_score`（延续分 0-100）就是为「持仓调仓」设计的，但从未被接进持仓管理**——它目前只落库观测，无人消费。你的新需求本质上就是让这个字段（或它的替代品）真正生效。

#### （B）LightGBM 三头 —— `quality_scorer.py` / `build_labels.py`

| 头 | 推理 | 标签定义 | 输出 |
|----|------|---------|------|
| 质量头 | `quality_scorer.py:798 score_one()` | `build_labels.py:140 label_one()` — 入场后 12 根 M5 内先触 +1R=1 / 先触 -1R=0 | `ai_score`（0-100） |
| 方向头 | `:850-911` | `build_labels.py:221 dir_label_one()` — **未来 24 根 M5 收盘相对 entry 的 ±0.8·ATR** | `ai_direction`(BUY/SELL/HOLD) + `ai_dir_prob` |
| 买点头 | `:916-938` | `build_labels.py:255 entry_label_one()` — 条件于方向的 R 触达 | `ai_entry`（0-1） |

**2026-09-01 新增方向头防抖**（`quality_scorer.py:789-911`）：`DIR_EMA_ALPHA=0.5` 概率 EMA + `DIR_MARGIN=0.15` 间隙带，解决三分类概率接近（实测 gap=0.077）导致的 argmax 瞬间跳转。

#### （C）闸门裁决 —— `quality_gate.py:253 decide()`

- 四动作：`VETO / DOWNGRADE / HOLD / UPGRADE`（`:223 adjust_grade()`）
- 铁律：**AI 绝不独立开出 HEXP 没给的方向**（`:243-250`、`:387-389`，`ai_opened` 恒 False）
- 耦合总分 = `w(k)·scorecard_total + (1-w(k))·c_ai`（`:323`）
- 2026-08-18 解耦：`calibrate_lm_score`（`ai_async_client.py:242-266`）——**运行期 C_ai 只取 LightGBM，DeepSeek 不参与融合**，DeepSeek 赋能改走离线训练管线（`ds_calib_weight` → sample_weight）

### 2.2 持仓 SL 侧：已有能力

#### （A）开仓 SL 决策链 —— `scheduler.py`

```
co_exec_sl_atr_mult (HEXP 引擎, hexp_engine.py:2258)
   ↓ :2399-2403
ai_sl_mult
   ↓ :2418-2435  会话键覆盖 close.<session>.trailing_stop_distance（亚/欧/美盘独立）
   ↓ :2470-2476  LightGBM 单源缩放 _ai_sl_scale（0.8~1.5×ATR）
   ↓ :2746       追击收紧 _tight_sl = min(ai_sl_mult × _chase, 1.8)   ← 硬封顶 1.8
   ↓ :2763-2773  显式算 sl_price / tp1
```

关键配置键：

| 键 | 值 | 位置 |
|----|-----|------|
| `hexp.exec.sl_atr_mult` | 2.0 | `hexp_engine.py:152` |
| `hexp.extreme.reversal_sl_atr_mult` | 0.5（极值区**收紧** SL，已有先例！） | `hexp_engine.py:255`、`:2248-2258` |
| `hexp.trend_start.sl_atr_mult` | 3.5（回测口径） | `hexp_engine.py:2718` |
| `close.<session>.trailing_stop_distance` | asia 1.5 / europe 2.0 / us 2.0 | `mt5_bridge.py:1056-1058`、`:4460` |

#### （B）盘中移动止损 —— `mt5_bridge.py:4426 _update_trailing_stops()`

- **5 秒周期**主循环（`:4164`），受 `close.trailing_stop_enabled` 开关控制（`:4442`）
- 逻辑：保本门槛（`breakeven_atr_mult`）→ 单线移动止盈（`trail_wide_atr_mult`）→ **棘轮：SL 只朝有利方向移动，绝不后退**（`:4438`）
- 仅处理**盈利侧**：盈利 > ATR × `breakeven_atr_mult` 才启动（`:4433-4434`）
- ⚠️ **浮亏持仓完全不在现有移动止损的管辖范围内** —— 这正是你要新增逻辑的空白地带

#### （C）盘中改单能力

| 能力 | 位置 | 说明 |
|------|------|------|
| 主号改 SL/TP | `mt5_bridge.py:4426` 内的 order_modify | 已有现成 5s 循环 + 棘轮 |
| 跟单号镜像 | `:2711 _modify_follower_position()` | 主号 SL 改动 → 自动镜像所有跟单号 |
| 主号事件广播 | `position_sync.py:438 _publish_master_sltp_event()` | 主桥消费后镜像 |
| 强制平仓 | `mt5_bridge.py:1875 _force_close_positions()` | 已有 |
| 部分平仓 | `mt5_bridge.py:2833 _partial_close_follower_position()` | 仅跟单侧 |

> ⚠️ **爆炸半径**：主号 SL 改动会经 `_publish_master_sltp_event` → `_modify_follower_position` **自动镜像到所有跟单账户**。这不是单账户动作。

### 2.3 数据基础（PG 实测，2026-09-02）

| 表 | 行数 | 覆盖 | 关键字段 |
|----|------|------|---------|
| `hcm_trading.positions` | **2012**（全部 `status='closed'`，2008 distinct ticket） | 2026-07-14 ~ 09-02（约 7 周） | open_price / current_price / **sl**(1975 条真实值) / tp / **float_profit** / open_time / signal_id |
| `hcm_trade.closed_positions` | **0（空表）** | — | — |
| `hcm_ai.labeled_samples` | 2565 | — | 现有三头训练基础 |
| `hcm_signal.signals` | 11081 | — | — |
| `hcm_market.klines_xauusd` M5 | 17487 | 2026-06-16 ~ 09-02 | — |
| 同上 M1 | 58977 | 2026-07-08 ~ 09-02 | — |
| 同上 H1 / H4 / D1 | 1772 / 941 / 626 | — | — |

> 📌 **重要修正**：`hcm_trade.closed_positions` 是空表，但 **`hcm_trading.positions` 实为「已平持仓记录表」**（2012 笔，一 ticket 一条，含开仓价/平仓价/SL/最终盈亏）。**LightGBM 路线的标签数据源是存在的**。

---

## 3. 三个关键发现（决定选型）

### 发现 1：现有三头存在「语义错位」，不能直接复用

现有质量头/方向头/买点头 **全部是「入场时点」模型**：

- 特征输入（`quality_features.py` FEATURE_COLS 25 维）是**信号产生时刻**的市场特征
- 标签基准是**信号 `entry_price`**（`build_labels.py:232`、`:246`）
- **特征里没有任何持仓状态维度**：无浮亏幅度、无持仓时长、无距 SL 距离、无 MFE/MAE

而你的需求问的是：**「这笔已经浮亏的持仓，接下来是反转还是回踩」**——基准是**当前价**，输入必须包含**持仓状态**。

→ 直接拿方向头 `ai_dir_prob` 当「反转概率」用，是**语义错配**：模型预测的是"未来 24 根相对（推理时刻）entry 的方向"，它不是为持仓浮亏场景训练的。

### 发现 2：「回踩放大 SL」与现有棘轮纪律直接冲突

`mt5_bridge.py:4438` 明确：**「棘轮：SL 只朝有利方向移动，绝不后退」**。

你的「回踩 → 放大 SL」本质是**让 SL 后退（放宽）**，这是棘轮纪律的例外通道。一旦开这个口子，若无强约束，极易滑向「亏损就放宽止损」的死亡螺旋。

### 发现 3：该逻辑让 AI 首次获得「主动平仓权」，越过现有红线

现有铁律（`quality_gate.py:3`、`:7`）：**AI 仅持否决/降级/升级权，无独立开仓权**。

「反转概率 > 40% → 缩 SL 小亏平仓」实质是 **AI 主动终结一笔持仓**。虽然方向上是"减仓/离场"（比开仓保守），但：
- 它是 AI 驱动的**写操作**（order_modify），不是否决
- 它会**真实产生已实现亏损**（把浮亏变实亏）
- 它会**镜像到所有跟单账户**

→ 属于需要你**显式红线授权**的新能力类别。

### 补充风险：与现有保本率结构的相互作用

此前链路审计结论：HEXP 主力路径 **81%~98% 以保本（profit=0）收场**（移动止损推保本）。

新逻辑两个分支都会动这个结构：
- **反转分支（缩 SL）**：把部分「本会保本」的单子 → 变成「小亏单」
- **回踩分支（放大 SL）**：把部分「本会保本」的单子 → 变成「更大亏损单」

→ **净效应未知，必须先用历史回放验证，不能凭直觉上线。**

---

## 4. 方案对比：LightGBM vs DeepSeek

| 维度 | DeepSeek 单独实现 | LightGBM 实现 |
|------|------------------|--------------|
| **冷启动** | ✅ 即时（改 prompt + 输出字段 + 触发条件） | ❌ 慢（需新建标签构造器 + 持仓状态特征 + 离线回放管线 + 训练） |
| **可复用现有代码** | ✅ 高：`ai_async_client.py` 骨架齐全（规则触发 `:365` / 异步不阻塞 / 超时降级 `:232` / 值域钳制 `:144` / 落库 `:333`） | ⚠️ 中：可复用 `auto_retrain.py` 重训闭环，但标签/特征需全新建 |
| **语义适配** | ✅ 天然理解「趋势末端 / 回踩 / 反转」这类概念 | ❌ 需重构标签定义（现有头全是入场时点模型，见发现 1） |
| **数据基础** | ✅ 不需要训练数据 | ⚠️ 边缘：2008 笔已平持仓，浮亏时点样本估 ~1200，**正样本（真反转）估 ~400**（偏少，过拟合风险） |
| **推理延迟** | ❌ 3~15s（`ai.ds.timeout_sec=15`）+ 限流风险 | ✅ <10ms 本地 |
| **调用成本** | ❌ 每笔持仓每次判定一次 API 调用 | ✅ 零边际成本 |
| **可高频** | ❌ 不可（成本 + 限流），需冷却期 | ✅ 可（每 5s 轮询都能跑） |
| **输出稳定性** | ❌ 同输入可能不同输出；LLM 概率是"自评置信度"，非频率学校准概率 | ✅ 确定性 |
| **"40% 阈值"的可靠性** | ⚠️ **LLM 给的 40% 不等于真实频率 40%**，需 isotonic 校准才可比 | ✅ 概率经 isotonic 校准（`quality_scorer.py:836-845`） |
| **可回测验证** | ❌ **无法用历史数据重放 LLM 判断**（历史时刻调不回来） | ✅ 可完整离线回测 |
| **失败降级** | ✅ 成熟（`FALLBACK` + 纯 HEXP 降级） | ✅ 成熟（`c_ai=None` → 透传） |

### 关键判断

**不是二选一，是时序问题：**

- **DeepSeek 的价值在「冷启动 + 语义理解」**：现在就能跑，能立刻开始**积累真实判定数据**
- **LightGBM 的价值在「可回测 + 可高频 + 零成本」**：但需要数据积累才能训好
- **DeepSeek 判定结果本身就是 LightGBM 最好的训练标签来源之一**（类似现有 `ds_calib_weight` 机制，`ai_async_client.py:254-257`）

---

## 5. 推荐方案：三阶段落地

### 阶段 0：离线回放验证（零实盘风险，强烈建议先做）

**做什么**：用 `hcm_trading.positions`（2012 笔已平）+ `klines_xauusd` M1/M5，离线回放每笔持仓的浮亏时点，统计：

1. 浮亏达 X 时，历史上最终「反转成亏损」vs「回踩后保本/盈利」的真实比例（**先验概率**——这直接决定 40% 阈值是否靠谱）
2. 若当时执行「缩 SL 到 0.5×ATR」策略 vs 「不动」，两种策略的**最终盈亏对比**
3. 不同触发阈值（0.3R / 0.5R / 0.8R）下的触发频次与效果

**产出**：一张「历史上这条规则到底是赚是亏」的答案表。**如果回放显示是亏的，整个需求就不需要上线了**——这是最便宜的证伪方式。

**成本**：纯离线读 PG + 写脚本，零实盘影响。

### 阶段 1：DeepSeek Shadow 模式（只判定，不动作）

**做什么**：

1. 新增持仓场景 prompt 模板 `build_position_prompt()`（并行于现有 `build_prompt()`，不动现有链路）
   - 输入增加持仓上下文：方向 / 开仓价 / 现价 / 浮亏 R 倍数 / 距 SL 距离 / 持仓时长 / 近 20 根 M5 OHLC 序列 / LightGBM 25 维特征（与 `ai_async_client.py:91-111` 同构注入）
2. 输出字段扩为 5 个（向后兼容，新增 2 个可选）：
   ```json
   {"fake_prob":..., "ai_sl_coeff":..., "continuity_score":...,
    "reversal_prob": <0~1 该浮亏为趋势反转的概率>,
    "pullback_prob": <0~1 为回踩的概率>}
   ```
   `parse_output()` 增加这两个键的钳制与越界告警（沿用 `:213-228` 模式）
3. 触发条件：持仓浮亏 ≥ `ai.rev.trigger_loss_r` 且持仓 ≥ `ai.rev.min_hold_bars` 根 M5 且距上次判定 > `ai.rev.cooldown_sec`
4. **`ai.rev.shadow_only = true`（默认）**：判定结果只落库（`hcm_ai.position_review` 新表） + 写 Redis，**绝不 order_modify、绝不改 SL**

**产出**：2~4 周真实判定数据，用于校准「40% 阈值」的真实含义 + 为阶段 2 提供训练样本。

### 阶段 2：LightGBM 第四头（reversal head）

**做什么**：

1. 新增 `build_reversal_labels.py`：对每笔持仓回放浮亏时点，标签 = 该时点后最终结局（反转=最终亏损 / 回踩=回踩后保本或盈利）
2. 新增持仓状态特征（现有 25 维之外）：浮亏 R 倍数 / 浮亏 ATR 倍数 / 持仓时长 / 距 SL 距离 / MFE / MAE / 回撤深度 / 自开仓起最高盈利回吐比
3. 训练 reversal head，接入 `quality_scorer.py:score_one()` 作为第四头
4. 阶段 1 积累的 DeepSeek 判定可作为 sample_weight（沿用 `ds_calib_weight` 机制）

**上线标准**：离线 AUC 达标 + 与阶段 0 回放结论一致 + 你确认后灰度。

### 阶段 3：灰度开启自动调 SL

仅当前两阶段验证通过后，才把 `ai.rev.shadow_only` 置 false，先小仓位/单品种灰度。

---

## 6. SL 键值设计（新增，全配置驱动，禁硬编码）

| 键 | 建议默认 | 用途 |
|----|---------|------|
| `ai.rev.enabled` | **false** | 总开关（默认关，需显式授权） |
| `ai.rev.shadow_only` | **true** | Shadow 模式：只判定不动作 |
| `ai.rev.trigger_loss_r` | 0.5 | 浮亏达 0.5R 才触发判定 |
| `ai.rev.min_hold_bars` | 2 | 持仓至少 2 根 M5 才判定（防刚开仓就判） |
| `ai.rev.cooldown_sec` | 300 | 同一持仓重复判定冷却（防 API 轰炸） |
| `ai.rev.max_review_per_position` | 3 | 单笔持仓最多判定次数 |
| `ai.rev.reversal_threshold` | **0.40** | 反转概率阈值（你要求的 40%） |
| `ai.rev.tighten_sl_atr_mult` | 0.5 | 判反转 → SL 收紧到 0.5×ATR（实现"小亏平仓"） |
| `ai.rev.pullback_sl_source` | `close.<session>.trailing_stop_distance` | 判回踩 → SL 放宽到**会话配置的 ATR 系数**（复用现有键，零新增语义） |
| `ai.rev.max_sl_atr_mult` | 3.0 | 放宽硬上限（**防死亡螺旋，不可绕过**） |
| `ai.rev.max_adjust_per_position` | 1 | 单笔持仓最多实际调整 SL 1 次 |
| `ai.rev.require_ratchet_whitelist` | true | 是否要求 SL 后退必须走白名单例外通道（对应发现 2） |

> 📌 关于「平仓配置的系数」：我理解为现有 `close.<session>.trailing_stop_distance`（asia 1.5 / europe 2.0 / us 2.0，`mt5_bridge.py:1056-1058`）。若你指的是别的键（如 `hexp.exec.sl_atr_mult=2.0`），请指出。

---

## 7. 改造点清单（阶段 1 范围，预估）

| # | 文件 | 改动 | 风险 |
|---|------|------|------|
| 1 | `hcm-signal-tower/signal_tower/ai_async_client.py` | 新增 `build_position_prompt()`；`parse_output()` 增 2 键钳制 | 低（纯新增，不动现有 `build_prompt`） |
| 2 | 新建 `hcm-signal-tower/signal_tower/position_reviewer.py` | 持仓浮亏轮询 + 触发判定 + 落库 + Redis 发布 | 中（新组件） |
| 3 | `tools/mt5_bridge.py` | （阶段 3 才动）在 `_update_trailing_stops` 之外新增 AI 调 SL 分支 | 高（改单动作） |
| 4 | 新建 PG 表 `hcm_ai.position_review` | 判定结果落库（阶段 0/1 观测用） | 低（新表，需授权） |
| 5 | 配置键写入 `hcm_config.metadata` | §6 的 12 个键，默认全关 | 低（需授权） |

**不改动**：`quality_gate.py`（不动现有闸门裁决）、`hexp_engine.py`（不动引擎）、`scheduler.py`（不动下单链）。

---

## 8. 风险与红线冲突汇总

| # | 风险 | 等级 | 缓解 |
|---|------|------|------|
| R1 | AI 首次获得主动平仓权，越过现有铁律 | 🔴 高 | 需你显式红线授权；默认 `shadow_only=true` |
| R2 | 与 81%~98% 保本率结构相互作用，净效应未知 | 🔴 高 | 阶段 0 离线回放先证伪 |
| R3 | 「回踩放大 SL」破坏棘轮纪律 → 死亡螺旋 | 🔴 高 | `max_sl_atr_mult=3.0` 硬顶 + `max_adjust_per_position=1` |
| R4 | LLM 的「40%」非频率学校准概率 | 🟡 中 | Shadow 期积累真实频率，isotonic 校准后再定阈值 |
| R5 | SL 改动镜像到所有跟单账户 | 🟡 中 | 爆炸半径须提前确认；可先限主号 |
| R6 | 训练样本少（~1200，正样本 ~400）过拟合 | 🟡 中 | 阶段 2 需严格时序切分 + PSI 监控（沿用 `auto_retrain.py` 机制） |
| R7 | DeepSeek 限流/超时导致判定缺失 | 🟢 低 | 沿用现有 FALLBACK 降级（`:27`、`:232`） |

---

## 9. 待你决策/授权清单

1. **§0 需求理解是否正确**（尤其第 4 条「平仓配置的系数」指哪个键）
2. **是否先做阶段 0 离线回放验证**（强烈建议，零风险，可能直接证伪）
3. **是否接受阶段 1 Shadow 模式优先**（只判定不动作，2~4 周）
4. **是否授权 R1 红线例外**（AI 主动终结持仓的新能力类别）
5. **爆炸半径**：自动调 SL 是否覆盖跟单账户，还是先限主号

---

## 附：上一问遗留回答 —— 「方向头 dir_head Buy 是哪儿来的」

`quality_scorer.py:850-911`：

1. `dir_model` 是**独立于 HEXP 的第三个 LightGBM 模型**，三分类，类序 `[-1, 0, 1]` = 空 / 观望 / 多（`:859`）
2. 各类概率经 isotonic 校准（`:860-876`），校准器退化时退回 raw 概率（`:871-872`）
3. `argmax` 得原始方向 `ai_direction_raw`（`:890`）
4. **2026-09-01 新增防抖**（`:883-911`）：概率 EMA（`DIR_EMA_ALPHA=0.5`）+ 间隙带（`DIR_MARGIN=0.15`，top1-top2 < 0.15 则维持前值不跳）→ 最终 `ai_direction` / `ai_dir_prob`

**答：dir_head 显示 Buy 与 hexp 无直接关系**——它是 dir_model 对「当前市场结构」的独立三分类预测（标签定义见 `build_labels.py:221`：未来 24 根 M5 收盘相对 entry ≥ +0.8·ATR 记 +1）。它只在 `quality_gate.py:335-351` 做「同向增强 / 反向否决」，且受 `ai.lm.direction_fuse` 开关控制（**默认 false，当前不参与裁决**）。HEXP 自己的方向来自 `hexp_engine.py`，两者是平行独立的两套判断。
